# Open Questions

- What exact iPadOS deployment target should the app use?
- Which AUv3 hosts are primary compatibility targets?
- Should transforms be stored as reversible history, baked edits, or both?
- What is the smallest useful LFO MVP: CC-only, probability-only, or both?
- How should clip delete behave in a one-clip MVP?
- Should pitch-class behavior apply before or after scale quantization when both are active?
- Should probability consume random values for muted/skipped/empty steps, or only playable steps?
- What project file format should be user-facing: JSON, package bundle, or app-private Codable data?
