# Development Log: 2026-06

## 2026-06-23

### Summary

Initialized the repository for an iPad-first AUv3 MIDI sequencer. Added project memory docs, MVP specs, a SwiftPM pure engine skeleton, and deterministic unit tests.

### Decisions

- Start engine-first with SwiftPM and XCTest.
- Keep AUv3 and SwiftUI integration behind the pure sequencing boundary.
- Model step locks as sparse overrides.
- Support pitch behavior through MIDI CC, channel routing, octave transposition, probability scaling, and harmony notes.

### Files / Areas Changed

- `AGENTS.md`
- `Package.swift`
- `Sources/MidiCore`
- `Sources/LockEngine`
- `Sources/TheoryEngine`
- `Sources/SequencerCore`
- `Sources/TransformEngine`
- `Tests/SequencerCoreTests`
- `docs`
- `specs/clip-midi-sequencer`

### Tests / Validation

Swift tests were added and passed. Validation command: `swift test`.

### Problems / Regressions

AUv3 plugin and SwiftUI app targets are not scaffolded yet. Persistence target is documented but not implemented.

### Next Steps

- Run `swift test`.
- Add project-level Codable models.
- Add delete transform semantics and LFO spec details.
- Create standalone/debug app target after engine behavior is broader.

## 2026-06-23

### Summary

Expanded active and backlog tasks into a streamlined implementation sequence from pure engine work to debug app, persistence, and AUv3 integration.

### Decisions

- Keep project-level Codable models as the next safe task.
- Treat LFO work as part of deterministic engine completion, constrained to MIDI-safe destinations.
- Defer SwiftUI and AUv3 work until project state, fixtures, and render-window behavior are stable.

### Files / Areas Changed

- `docs/tasks/active.md`
- `docs/tasks/backlog.md`
- `docs/devlog/2026-06.md`

### Tests / Validation

Documentation-only change. No tests were run.

### Problems / Regressions

No code changed. The task stream still needs future implementation work to keep the roadmap honest.

### Next Steps

- Add project-level Codable models and JSON round-trip tests.
- Add explicit transform command metadata.
- Define delete semantics before exposing delete in UI.
