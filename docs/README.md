# MidiSequencer Docs

Durable project memory for the AUv3 MIDI sequencer.

- Start with `AGENTS.md`.
- Check `docs/tasks/active.md` before new work.
- Use `specs/clip-midi-sequencer` for the MVP product and engineering spec.
- Record meaningful architecture decisions in `docs/adr`.
- Update `docs/devlog` after substantial sessions.
